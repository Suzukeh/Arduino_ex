void setup() {
  // スラッシュ二つを先頭に書いた行はコメントアウトになる。プログラムの挙動には影響しないメモ書きなどに使う。
  // setup関数はプログラムの起動時（リセット時）に一度だけ実行される。
  // 主な用途はピンの設定など。
}

void loop() {
  // loop関数はsetup関数が終わった後に何度も繰り返し実行される。Arduinoが止まらない限り動作し続ける。
}